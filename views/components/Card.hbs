  <article class="card mb-2">
    <div class="card-body">
      <p>{{item.origin}}</p>
      <p>{{item.shortUrl}}</p>
      <a class="btn btn-danger">Eliminar</a>
      <a class="btn btn-warning">Editar</a>
      <a class="btn btn-info">Copiar</a>
    </div>
  </article>